<%= form_for(@portfilo) do |f| %>
  <% if @portfilo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@portfilo.errors.count, "error") %> prohibited this portfilo from being saved:</h2>

      <ul>
      <% @portfilo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :info %><br>
    <%= f.text_field :info %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
